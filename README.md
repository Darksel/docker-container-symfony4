docker-container-symfony 4
==========================

##Prérequis
Docker Engine et Docker Compose doivent être installé

##Installation des conteneurs dockers
```
$ docker-compose build
$ docker-compose up -d
```


